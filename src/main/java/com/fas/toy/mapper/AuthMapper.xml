<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.fas.toy.dao.auth.AuthDao">
  <select id="searchKakaoAuths" resultType="com.fas.toy.dto.auth.SearchKakaoAuthOut">
    SELECT COUNT(uid) AS CNT FROM member
      WHERE id = #{email}
      <!-- AND kakao_id = #{id} -->
  </select>

  <insert id="registWithKakao">
    INSERT INTO member (userId, name, nickname, password, profileImage)
      VALUES (#{email}, #{nickName}, #{nickName}, "12341234", #{profileImg})
  </insert>
</mapper>